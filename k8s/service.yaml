apiVersion: v1
kind: Service
metadata:
  name: train-service
  labels:
    app: train-service
spec:
  type: LoadBalancer
  selector:
    app: train-service
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4000
      name: http
